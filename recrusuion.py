#num=int(input("enter the number"))
#def factorial(num):
    #if num==1 or num==0: # base case to stop recursion, otherwise we get recursion error 
        #return 1
    #else:
        #return num*factorial(num-1)
#f=torial(num)
#print(f)

#sum of n natural number
num=int(input("enter the number"))
def sum(num):
    
    if num==1 or num==0
    return num+sum(num-1)
s=sum(num)
print(s)